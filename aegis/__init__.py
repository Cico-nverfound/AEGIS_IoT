"""
AEGIS — A smart, context-aware security brain for the home.

Not another firewall. Not another Pi-hole.
A local security layer that learns what "normal" looks like,
detects when device behaviour changes, explains why, and acts.

PoC package: network telemetry -> behavioural baselines ->
anomaly detection -> AI-assisted reasoning -> deterministic
policy decision -> active response (block / quarantine / alert).
"""

__version__ = "0.1.0-poc"
